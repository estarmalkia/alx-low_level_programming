#include "main.h"
#include <stdio.h>

/**
 * main - check the code 
 *
 * Return: Always 0.
 */
int main(void)
{
  char s[] = "Holberton School!\n";
  char *ptr;

  p = string_toupper(s);
  printf("%s", ptr);
  printf("%s", s);
  return (0);
}
